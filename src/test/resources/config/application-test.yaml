spring:
  datasource:
    url: jdbc:tc:postgresql:16:///databasename?currentSchema=public
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create