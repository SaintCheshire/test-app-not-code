spring:
  datasource:
    url: jdbc:tc:postgresql:16:///databasename?currentSchema=public
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create

logging:
  level:
    saint.cheshire.test_app_not_code_volume_1.web.filter.LoggingFilter: INFO